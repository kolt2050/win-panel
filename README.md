<p align="center">
  <img src="docs/icon.png" alt="WinPanel" width="128" height="128">
</p>

<h1 align="center">WinPanel</h1>

<p align="center">
  <b>Плавающая панель быстрого запуска для Windows</b><br>
  Кастомный док, который всегда поверх всех окон
</p>

<p align="center">
  <img src="https://img.shields.io/badge/.NET-8.0-512BD4?style=flat-square&logo=dotnet" alt=".NET 8">
  <img src="https://img.shields.io/badge/Platform-Windows-0078D6?style=flat-square&logo=windows" alt="Windows">
  <img src="https://img.shields.io/badge/UI-WPF-68217A?style=flat-square" alt="WPF">
</p>

---

## 📋 Описание

**WinPanel** — это легковесная плавающая панель для быстрого запуска приложений в Windows. Панель отображается поверх всех окон и позволяет закреплять ярлыки программ методом drag-and-drop, как на панели задач Windows или macOS Dock.

### ✨ Ключевые особенности

- 🎯 **Always on Top** — панель всегда видна поверх других окон
- 🖱️ **Drag & Drop** — просто перетащите `.exe`, `.lnk` или `.url` файлы на панель
- 🎨 **Прозрачность** — регулируемая прозрачность от 20% до 100%
- 📐 **Ориентация** — горизонтальное или вертикальное расположение ярлыков
- 🔍 **Масштабирование** — изменение размера панели с помощью мыши (Resize Grip)
- ⚙️ **Автостарт** — опция запуска вместе с Windows
- 🖱️ **Handle-зона** — выделенная область для удобного доступа к настройкам
- 💾 **Автосохранение** — все настройки сохраняются автоматически
- 🖼️ **Иконки** — автоматическое извлечение иконок из исполняемых файлов
- 🪶 **Легковесность** — минимальное потребление ресурсов

---

## 🚀 Быстрый старт

### Требования

- Windows 10/11 (x64)
- Для сборки: [.NET 8 SDK](https://dotnet.microsoft.com/download/dotnet/8.0)

### Установка

1. Скачайте `WinPanel.exe` из [Releases](../../releases)
2. Запустите — панель появится в верхней части экрана
3. Готово! Начните перетаскивать ярлыки программ на панель

### Сборка из исходников

Основные команды для сборки приложения:

```bash
# Обычная сборка (Debug)
dotnet build

# Публикация в один файл (Single-file, ReadyToRun)
# Это рекомендуемый способ для получения готового EXE
dotnet publish -c Release -r win-x64 --self-contained false /p:PublishSingleFile=true /p:PublishReadyToRun=true

# Публикация со всеми зависимостями (Self-contained)
# EXE будет работать даже без установленного .NET на ПК
dotnet publish -c Release -r win-x64 --self-contained true /p:PublishSingleFile=true
```

**EXE файл будет находиться в:**
`bin/Release/net8.0-windows/win-x64/publish/WinPanel.exe`

---

## 📖 Использование

### Добавление ярлыков

| Действие | Описание |
|----------|----------|
| **Перетаскивание** | Перетащите `.exe`, `.lnk` или `.url` файл на панель |
| **Поддерживаемые форматы** | Исполняемые файлы (`.exe`), ярлыки Windows (`.lnk`), URL-ярлыки (`.url`) |

### Управление ярлыками

| Действие | Результат |
|----------|-----------|
| **Клик левой кнопкой** | Запуск программы |
| **Клик правой кнопкой по ярлыку** | Контекстное меню ярлыка |
| **Перетаскивание зажатой иконки** | Изменение порядка ярлыков на панели |

#### Контекстное меню ярлыка:
- 🚀 **Запустить** — запустить программу
- 📂 **Открыть расположение** — открыть папку с файлом в проводнике
- ❌ **Удалить** — удалить ярлык с панели

### Настройки панели

Чтобы открыть настройки всей панели, нажмите **правой кнопкой мыши** по **пустому месту** или специально выделенной **Handle-зоне** (три точки в начале панели).

| Опция | Описание |
|-------|----------|
| **📐 Ориентация** | Переключение между горизонтальным и вертикальным расположением |
| **⚙️ Автозагрузка** | Включение/выключение запуска при старте Windows |
| **👁 Прозрачность** | Слайдер для регулировки прозрачности (20-100%) |
| **🗑️ Полное удаление** | Удаление приложения, конфигов и записи в реестре |
| **❌ Закрыть** | Закрыть приложение |

### Изменение размера и перемещение

- **Перемещение**: Зажмите **левую кнопку мыши** на самой панели или Handle-зоне и перетащите.
- **Масштаб**: Зажмите и тяните за **Resize Grip** (полоски в нижнем правом углу) для изменения размера иконок и панели.

---

## 🗑️ Полное удаление

При выборе пункта **"Полное удаление"** в контекстном меню панели, приложение выполняет следующие действия:

1.  **Реестр**: Удаляет ключ автозагрузки `HKCU\Software\Microsoft\Windows\CurrentVersion\Run\WinPanel`.
2.  **Конфигурация**: Полностью удаляет папку с настройками и списком ярлыков: `%AppData%\WinPanel`.
3.  **Исполняемый файл**: Пытается удалить сам файл `WinPanel.exe` (через временную команду CMD после закрытия).

---

## ⚙️ Конфигурация

Все настройки автоматически сохраняются в файле:

```
%AppData%\WinPanel\config.json
```

### Структура config.json

```json
{
  "WindowLeft": 500.0,
  "WindowTop": 50.0,
  "Opacity": 80.0,
  "IsVertical": false,
  "ShortcutPaths": [
    "C:\\Program Files\\App1\\app.exe",
    "C:\\Users\\User\\Desktop\\App2.lnk"
  ]
}
```

| Параметр | Тип | Описание |
|----------|-----|----------|
| `WindowLeft` | double | X-координата окна |
| `WindowTop` | double | Y-координата окна |
| `Opacity` | double | Прозрачность (20-100) |
| `IsVertical` | bool | Вертикальная ориентация |
| `ShortcutPaths` | string[] | Список путей к закреплённым программам |

---

## 🏗️ Архитектура проекта

```
win-panel/
├── WinPanel.csproj          # Файл проекта .NET 8
├── App.xaml                 # Точка входа приложения
├── App.xaml.cs
├── MainWindow.xaml          # UI главного окна
├── MainWindow.xaml.cs       # Логика главного окна
├── icon.ico                 # Иконка приложения
├── icon.png                 # Иконка (PNG)
├── Models/
│   └── ShortcutItem.cs      # Модель ярлыка
├── Services/
│   ├── IconExtractor.cs     # Извлечение иконок из файлов
│   └── ConfigService.cs     # Сервис сохранения настроек
└── docs/
    └── icon.png             # Иконка для документации
```

---

## 🛠️ Технологии

- **Фреймворк**: .NET 8
- **UI**: WPF (Windows Presentation Foundation)
- **Сериализация**: System.Text.Json
- **Извлечение иконок**: Shell32 API (SHGetFileInfo)

---

## 📝 Лицензия

MIT License — свободное использование и модификация.

---

## 🤝 Вклад в проект

Приветствуются Pull Request'ы и Issue! 

1. Fork репозитория
2. Создайте ветку для фичи (`git checkout -b feature/amazing-feature`)
3. Commit изменений (`git commit -m 'Add amazing feature'`)
4. Push в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request